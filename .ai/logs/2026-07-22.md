# Agent Log — 2026-07-22 — algolia — scope Algolia search by version/framework/Connect

---
timestamp: 2026-07-22T09:00:00Z
action: audited existing Algolia search setup (indexing vs. query-time behavior)
context: user asked for a full audit of how search currently works, ahead of migrating
  from manual Docker-based crawling to automatic crawling, and adding Connect + 5.35.0
  scoping
outcome: |
  Traced the full pipeline: manual `algolia/docsearch-scraper` Docker runs (documented in
  algolia-search.md) populate an Algolia index from `search-config.json`/`search-config-dev.json`
  crawl rules; live search queries hit Algolia's API directly from the browser via
  `src/theme/SearchBar/index.js` (@docsearch/react) and `src/theme/SearchPage/index.js`
  (algoliasearch-helper), using credentials from each `docusaurus.config*.js`'s `algolia`
  block. Per-page scoping is custom (not Docusaurus's built-in contextualSearch) via
  `src/components/Search/util.ts`'s getLocationInfo/getCustomFacets, which only ever pushed
  a `framework` facet — a computed `docsBase` (docs vs connect) was dead code, never used.
  Confirmed Connect was never crawled (`start_urls` never included `/connect/...`) and
  5.35.0 was actively blocked (`stop_urls` regex blocked all numbered-version paths) —
  both entirely unindexed. Confirmed via `build/sitemap.xml` that Docusaurus's own sitemap
  generation already lists both 5.35.0 and Connect URLs correctly, so sitemap coverage was
  never the gap — only the crawl config's own start_urls/stop_urls rules were.
learnings: this repo's search does NOT use Docusaurus's built-in `contextualSearch`
  facet-filter mechanism for version/instance scoping, despite the official Docusaurus
  DocSearch integration guide recommending exactly that (docusaurus_tag/version facets
  auto-injected via meta tags). Worth remembering when reasoning about this repo's search
  behavior — the custom `util.ts` facet layer is the actual source of truth, not
  contextualSearch.
---

---
timestamp: 2026-07-22T10:00:00Z
action: implemented dev-scoped search facet logic + dev crawl coverage, then extended to
  prod/staging after user request
context: user wanted, first, only the dev environment updated so that being on the
  homepage searches Connect + 6.1.0 + 5.35.0 together, being on a Connect page searches
  Connect only, being on a v6 framework page searches that framework/version only, and
  being on a v5.35.0 framework page searches that framework/version only — then asked to
  propagate the same crawl-config schema change to "staging, prod, and any other
  environment not yet touched"
outcome: |
  Rewrote `src/components/Search/util.ts`: getLocationInfo now also detects the 5.35.0 URL
  segment; a new getDocusaurusTag() helper builds the `docs-<pluginId>-<versionName>` tag
  Docusaurus itself uses internally (docs-default-6.1.0 / docs-default-5.35.0 /
  docs-connect-current), returning null on the homepage so no tag restriction applies
  there; getCustomFacets() now applies docusaurus_tag + framework facets only where
  appropriate. Verified live (not just by code review): ran `npm run dev`, opened the
  search modal/full search page from all four locations, and inspected the actual
  outgoing Algolia request body via Playwright network intercepts — facetFilters matched
  the spec exactly in all four cases (homepage: `["type:content"]` only; v6 framework
  page: adds `docusaurus_tag:docs-default-6.1.0` + `framework:<fw>`; 5.35.0 page: same
  with `docs-default-5.35.0`; connect page: `docusaurus_tag:docs-connect-current` only).
  Extended `search-config.json` (prod) and `search-config-dev.json` (dev) with matching
  `start_urls` for 5.35.0 and Connect, and removed the `stop_urls` entry blocking
  numbered-version paths. Set `contextualSearch: false` on every `docusaurus.config*.js`
  algolia block (js/staging/dev/localhost/localdev) so Docusaurus's automatic contextual
  facet filters can't override the custom homepage-unrestricted behavior.
  No crawl has actually been run with any of these config changes yet — the shared index
  still only contains the pre-existing v6.1.0-only content.
learnings: |
  - During manual testing the user found `docusaurus.config.localhost.js`/`localdev.js`'s
    dev Algolia app (`appId: 'K01Y8I3I2D'`) does not work ("does not exist or wrong") —
    it appears to be dead/stale, not something this session broke. `docusaurus.config.js`,
    `docusaurus.config.staging.js`, and `docusaurus.config.dev.js` all already point at the
    same single working app/index (`3NDU900UQH` / `docs_mobiscroll`) — a pre-existing
    condition, meaning prod/staging/dev currently share one search dataset with no real
    separation. This is worth surfacing to whoever owns the Algolia account — genuine
    dev/prod isolation for search would require provisioning a real second app+index.
  - Corrected a wrong claim made mid-session: crawling with dev-domain URLs into the
    shared index would NOT cause production visitors to get redirected to
    dev.mobiscroll.com on click. Verified via
    `node_modules/@docusaurus/theme-search-algolia/lib/client/useSearchResultUrlProcessor.js` —
    Docusaurus always strips the domain from a search result URL (keeping only
    pathname+search+hash) and navigates via the client-side router on whatever origin is
    currently active, unless `externalUrlRegex` is configured (it isn't, anywhere in this
    repo). The real risk of cross-environment crawling is content mismatch (a path
    indexed from dev that doesn't exist yet on prod → dead link/404 on click), not a
    domain redirect.
---

---
timestamp: 2026-07-22T11:00:00Z
action: clarified for the user how search-config.json/search-config-dev.json relate to
  the crawl and to automatic-crawling options — no file/code changes
context: user asked a series of follow-up questions to build an accurate mental model
  before deciding whether to run a manual crawl: what the config affects (indexing vs.
  query time), whether search-config.json is "seen" via the build/deployment at all, and
  how each of the two automation options (Algolia-hosted Crawler vs. CI-automated Docker)
  would relate to this file
outcome: |
  Explained the config file is never fetched by anything automatically — it's read off
  local disk, piped through jq into a `.env` value, and handed to the docsearch-scraper
  Docker container at the moment a human runs the command; the container then makes real
  outbound HTTP requests to whatever's already live at those URLs. It has no relationship
  to this repo's build/deploy pipeline at all.
learnings: the two "automatic crawling" options differ in whether this repo's JSON file
  stays authoritative going forward — worth remembering before recommending either one:
  - Algolia-hosted Crawler: rules get manually re-entered into Algolia's own dashboard/API
    once; this repo's search-config.json then becomes disconnected/stale unless someone
    remembers to mirror future edits into Algolia's config by hand (a real drift risk).
  - CI-automated Docker (e.g. GitHub Actions cron): keeps this exact file as the live
    source of truth — a scheduled job just checks out the repo and re-runs the same
    jq+docker steps a human would, so whatever's committed here is what runs, automatically.
---

---
timestamp: 2026-07-22T14:00:00Z
action: added homepage OR-facet scoping (Connect + current v6 UI, excluding 5.35.0); noted
  a version-hardcoding future consideration — no implementation done for the latter
context: user changed the homepage requirement from "search everything" to "Connect +
  latest v6 UI only" (5.35.0 excluded), then asked whether there's a way to avoid
  hand-updating util.ts's hardcoded version strings whenever versions change
outcome: |
  Implemented the new homepage behavior in `src/components/Search/util.ts`:
  getDocusaurusTag() now returns an array (`["docs-connect-current",
  "docs-default-6.1.0"]`) specifically for the homepage case, and getCustomFacets() wraps
  an array-valued tag as a nested OR group in Algolia's facetFilters (top-level array =
  AND, nested array = OR). Added a `CURRENT_VERSION` constant to avoid a second hardcoded
  "6.1.0" literal. Fixed a resulting TS error (facets array needed an explicit
  `(string | string[])[]` type). Verified live via a fresh dev server + Playwright network
  intercept: homepage now sends
  `facetFilters: ["type:content", ["docusaurus_tag:docs-connect-current",
  "docusaurus_tag:docs-default-6.1.0"]]` — 5.35.0 excluded as intended, other pages
  unaffected.
learnings: |
  FUTURE CONSIDERATION (explicitly not implemented — user asked only to log this, not
  build it): `util.ts` still hardcodes version strings (`CURRENT_VERSION = "6.1.0"`, the
  `/\/5\.35\.0\//` regex) that will need manual updates on every future version bump.
  Proposed but deferred approach: derive these from Docusaurus's own version metadata via
  `useVersions('default')` (from `@docusaurus/plugin-content-docs/client`), which already
  reflects `lastVersion`/`onlyIncludeVersions` in `docusaurus.config.js` — the one place
  version bumps already have to happen (see Workflow H). That hook returns each version's
  name/path plus an `isLast` flag, so "current" vs "other" could be computed generically
  instead of hardcoded. Tradeoff: `getLocationInfo`/`getCustomFacets` are currently plain
  functions taking only `location`; this would require turning them into hooks (or having
  `SearchBar`/`SearchPage` call `useVersions()` and pass the result in), a real but modest
  refactor — worth revisiting if/when another version bump is planned.
---
