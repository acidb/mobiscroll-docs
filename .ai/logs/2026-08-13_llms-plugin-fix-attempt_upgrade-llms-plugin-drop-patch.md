# Agent Log — 2026-08-13/14 — llms-plugin-fix-attempt — upgrade docusaurus-plugin-llms, drop local patch

---
timestamp: 2026-08-14T00:00:00Z
action: investigated whether docusaurus-plugin-llms@0.5.1 (npm latest) still needed the local patch-package patch pinned against 0.3.0
context: user wanted to test if upgrading the plugin would let us drop patches/docusaurus-plugin-llms+0.3.1.patch, which works around the plugin's HTML-tag-stripping regex corrupting JSX/HTML tags inside fenced code blocks in generated llms.txt/llms-full.txt output
outcome: ran each version's real cleanMarkdownContent/maskCodeSegments/resolvePartialImports functions against real project files (not just source reading). Found 0.5.1's fenced-block masking still breaks on CRLF line endings (lookahead `(?=\n|$)` doesn't tolerate `\r`), and separately found 0.5.1 adds a blanket rule stripping any PascalCase JSX tag, affecting <CodeBlock>/<Tabs>/<TabItem> usage in docs/_shared partials and per-framework guides
learnings: all 1,136 committed .md files are LF-only in git; CRLF only appeared locally due to core.autocrlf=true — the bug's reproducibility depends on the building machine's git config, not file content, so the real fix is forcing LF via .gitattributes rather than relying on any contributor's local config
---

---
timestamp: 2026-08-14T00:30:00Z
action: discovered <CodeBlock> usage in the 12 docs/_shared partials cannot be converted to plain markdown fences
context: initial plan was to swap <CodeBlock language="X">...</CodeBlock> for plain ```X fences (identical rendering, survives the plugin's masking). Reading the actual files showed every one relies on {props.framework} — a real JSX expression evaluated live per-framework (e.g. <InstallNpm framework="react" />) — which a plain markdown fence does not evaluate (MDX treats fence content as literal text)
outcome: converting would have broken live per-framework substitution across all 5 frameworks' install/upgrade/timezone pages. Confirmed instead that 0.5.1 only strips the <CodeBlock>/</CodeBlock> tag markup, not the text between them, so the framework substitution (done separately by scripts/strip-jsx.js via {props.framework} replace) still works after upgrading — the only loss is strip-jsx.js's fence-reconstruction trigger, since it keyed off the tag which is now gone before strip-jsx.js runs
learnings: always verify a "safe, identical-rendering" migration claim by reading the actual source files, not just diffing plugin output in isolation — resolvePartialImports (which inlines partial imports like <Accessibility /> before cleanMarkdownContent runs) can mask real risk in isolated function tests
---

---
timestamp: 2026-08-14T01:00:00Z
action: implemented the upgrade — broadened .gitattributes, added llms-fence HTML comment markers around all 20 <CodeBlock> usages (12 docs/_shared partials + 12 versioned_docs/version-6.1.0/_shared mirrors, kept in sync per project convention), rewrote scripts/strip-jsx.js's fence-reconstruction to key off the markers instead of the (now-plugin-stripped) <CodeBlock> tag, removed dead <Tabs>/<TabItem> handling (accepted per user decision: tab labels are lost from LLM output, code content survives since it's usually its own fenced block already), bumped docusaurus-plugin-llms to 0.5.1 in package.json, deleted patches/docusaurus-plugin-llms+0.3.1.patch, ran npm install
outcome: full npm run build succeeded. Verified in build output: <div>/<span> tags inside JSX code blocks survive (original CRLF bug fixed); all 20 CodeBlock partials render as proper fenced blocks with correct per-framework substitution (mobiscroll config react/angular/vue/js/jquery) across all six generated llms-*-full.txt files; Tabs/TabItem content (e.g. Angular FullCalendar migration guide) preserved with labels dropped as expected; no stray <Tabs>/<CodeBlock>/llms-fence marker text leaked into any generated output. versioned_docs/version-5.35.0 (frozen historical version, out of scope) shows the same cosmetic CodeBlock-unfencing as an accepted side effect, since its shared partials weren't touched
learnings: `git add --renormalize .` run without a path filter will renormalize ALL text files git considers text-eligible (caught it touching static/img/calendar.svg, unrelated to this task, and reverted it) — always review the staged diff stat after a bare renormalize before committing. Also: .gitattributes eol=lf only affects future checkouts, not files already on disk — had to manually normalize the working tree's existing CRLF files to actually verify the build locally in the same session
---

---
timestamp: 2026-08-14T02:00:00Z
action: researched whether it's safe to loosen the plugin pin from exact "0.5.1" to "^0.5.1"
context: user wanted to know if floating patch versions was safer than the CodeBlock/patch-package situation just fixed
outcome: pulled every published version from npm, diffed cleanMarkdownContent across 0.3.0→0.5.1, read all 10 GitHub releases, and searched the issue tracker (rachfop/docusaurus-plugin-llms). Found v0.4.0 shipped a self-declared "Breaking Change" (addMdExtension default) in a minor bump; v0.5.0 silently introduced the blanket PascalCase-tag-stripping behavior with zero changelog mention; two open, unresolved issues (#64, #65, both filed 2026-08-01, zero maintainer response) independently report the exact "silent content loss" bug class we hit, from other users. ^0.5.1 resolves to >=0.5.1 <0.6.0 (verified via node's semver lib) so it can't jump to a hypothetical 0.6.0, but 0.5.0→0.5.1 itself already contained multiple non-trivial path/behavior changes framed as "bug fixes"
learnings: recommended keeping the exact pin (no caret) given this plugin's demonstrated pattern of undocumented behavior changes even in patch releases; flagged that any future upgrade should repeat the full manual build+diff verification rather than being trusted to a routine `npm install`
---

---
timestamp: 2026-08-14T02:30:00Z
action: found and fixed a second, broader instance of the 0.5.1 PascalCase-stripping bug, missed by the original verification pass
context: while answering user's follow-up ("is 0.5.1 actually better than the old patch"), re-examined GitHub issue #64 more carefully — it covers ANY self-closing component with prop-carried content, not just <CodeBlock>/<Tabs>. Built a source-level auditor (masks fenced blocks AND inline code spans like the plugin's real maskCodeSegments, then flags self-closing PascalCase tags not covered by resolvePartialImports or already-handled cases) and ran it against the whole docs/connect tree
outcome: found <DocsLink>, <FileBlock>, <McpConfigBlock>, <DocsUrl>, <McpCliBlock>, <McpUrl> — locally-defined components in the 7 ai-integration.md/postman-collection.md pages carrying their entire output in props (download links, MCP JSON config, fetched file content) — losing that content entirely under 0.5.1, confirmed via a real 0.3.0-vs-0.5.1 build diff (temporarily reverted via git stash to build the 0.3.0+patch baseline, then restored). Old behavior left raw unrendered JSX litter in the output (uglier but the filename/path info was technically recoverable); new behavior removes it with no trace — a real regression in information content, not just formatting
outcome: fixed by extending the same marker technique: inserted {/* llms:TYPE;params */} MDX comments (not <!-- --> HTML comments — those failed MDX compilation when placed inline next to JSX; and not `|`-separated params — that broke GFM table cell parsing, since these markers sit inside table cells) around all 6 components across 12 files (5 ai-integration.md + 5 versioned_docs/version-6.1.0 mirrors + connect's ai-integration.md; postman-collection.md's <PostmanRunButton> has no textual payload, added to strip-jsx.js's existing silently-dropped UI-chrome set instead). Extended scripts/strip-jsx.js to reconstruct: docsurl/mcpurl as inline code URLs, mcpconfig/mcpcli as fenced JSON/bash blocks computed from the same logic as the live components, fileblock by reading the actual static/ file and inlining its content, docslink as a markdown link (using surviving child text, then filename, then path basename as the label fallback)
outcome: full rebuild verified clean across all 6 llms-*-full.txt outputs: zero empty DocsLink table rows, correct JSON/bash blocks, actual file content inlined, no stray tags or markers anywhere in build output; previously-verified CRLF and CodeBlock fixes still intact
learnings: two hard-won syntax lessons for any future MDX-invisible marker: (1) standard HTML comments <!-- --> are not reliably parsed by MDX3 when adjacent to real JSX on the same line (only safe as their own standalone block) — use {/* */} instead; (2) never use `|` inside a marker that might sit inside a markdown table cell — GFM splits table columns on unescaped `|`, which then cascades into confusing MDX's brace-matching too. Also: don't trust a single "grep for the tags I already know about" verification — the same bug class had a second, non-obvious blast radius (self-closing components with prop-only payload) that only surfaced by building a proper source-level auditor and asking "what else matches this failure shape?"
---

---
timestamp: 2026-08-14T03:00:00Z
action: did a real production-vs-localhost diff at the user's request, found two more real discrepancies, fixed one, accepted the other
context: user asked to fetch mobiscroll.com/docs/llms-javascript-full.txt, llms-angular-full.txt, and individual .md pages (ai-integration.md, upgrade-guide-v6.md) and byte-diff them against a fresh local build (served via `npm run serve` on a free port, since 3000 was already occupied by something else)
outcome (path structure): production serves individual .md page twins at a doubled path (/docs/docs/{framework}/...) — confirmed as GitHub issue #59's "baseUrl applied twice on disk" bug, already fixed in 0.5.1; our build correctly collapses it to the real single path. Each version is internally self-consistent (TOC links match wherever the file actually lives) — not a bug, but a real URL-scheme change that will 404 any previously-cached doubled-path link once this deploys
outcome (ImgComparisonSlider leak — fixed): found <ImgComparisonSlider> (a before/after image slider widget, already handled by strip-jsx.js's BLOCK_REMOVE_TAGS to drop entirely — no LLM-relevant text) now leaking its raw <figure>/<img>/<svg>/<path> children into output, same root cause as everything else: 0.5.1 strips the wrapper tag before strip-jsx.js's tag-name-based detection ever sees it. 145 occurrences across 25 files (5 frameworks × agenda/calendar/scheduler/templating/timeline) + their versioned_docs/version-6.1.0 mirrors (50 files, 300 markers total, since each occurrence needs an open+close marker). Fixed with the same technique: {/* llms:blockremove */} / {/* /llms:blockremove */} markers around each usage (confirmed via full-file audit that open/close tags are always alone on their own line, no self-closing or same-line variants), new inMarkerBlockRemove state in strip-jsx.js's line loop. Verified: 0 figure/svg/marker leaks across all 6 frameworks post-fix
outcome (import-line stripping — accepted, not fixed): found real code examples losing their `import` line entirely — e.g. `import { print } from '@mobiscroll/print';` vanishing from inside an actual fenced code block. Root cause is structurally different from every other bug this session: resolvePartialImports (0.5.1's content.js) does `resolvedPartial.replace(/^\s*import\s+.*$/gm, '')` to strip a partial's OWN mdx import declarations before splicing it into the parent page — but this runs *before* cleanMarkdownContent's maskCodeSegments even exists, so it has zero fence-awareness and nukes any line starting with "import" anywhere in the partial, including inside real copy-paste code samples. Unfixable via our marker technique: there is no way to make a real, valid JS import line not start with the literal word "import", so nothing survives to reconstruct from. Scope: 28 lines across 27 files (docs/_shared/eventcalendar/{cal-int-server,print-install-import}.mdx, and 25 auto-generated eventcalendar/options*.md files across all 5 frameworks, all showing the print-module-via-modules-option example). Presented options to the user (small targeted patch-package patch vs. accept vs. hold off entirely) — user chose to accept the loss for now rather than reintroduce a patch
learnings: comparing against the actual deployed production site (not just before/after diffing our own local builds) surfaced two failure shapes our own audits had missed — a wrapping tag whose entire block (not just itself) needs dropping, and a plugin-internal step (resolvePartialImports) that runs before any fence protection exists at all. Neither showed up in the earlier `<CodeBlock>`/self-closing-component audits because they're structurally different failure shapes, not more instances of the same one. Lesson: a "verified" migration should include a real diff against whatever is actually live, not just internal before/after comparisons of the same local pipeline
---

---
timestamp: 2026-08-18T11:00:00Z
action: broader production-vs-localhost comparison (4 full-text files + 30 varied individual pages across all 5 frameworks + connect), found and fixed one more real bug
context: user asked to compare mobiscroll.com against a fresh localhost build across a deliberately wide, non-repeating sample (30 pages, no topic repeated across frameworks) plus llms-{javascript,angular,react,connect}-full.txt, and report both URL reachability and content differences
outcome: all 34 URLs work locally once two of my own test-path guesses were corrected — jquery/grid-layout.md has no "gridlayout/" folder segment, and connect pages carrying a custom frontmatter `slug` (oauth, node-sdk, scopes, postman-collection) land at a doubled connect/connect/... path locally (self-consistent — TOC links match — but a real quirk, noted for awareness, not fixed). 18 of 30 pages were byte-identical; the rest matched only already-known/accepted categories (DocsLink/DocsUrl/McpConfigBlock fixes, Tab label loss, import-line loss). jquery/grid-layout.md has no .md twin on production at all (returns Docusaurus's soft-404 homepage shell at HTTP 200) — a content-freshness gap between production's last deploy and current HEAD, unrelated to this upgrade; our build actually generates it correctly
outcome (NEW bug found and fixed): `<Parameter defaultValue={<code>value</code>}>` — 0.5.1's tag-stripping regex matches up to the *first* '>' it finds, which for a nested `<code>` tag inside a prop value is the inner tag's, not the outer `<Parameter>`'s — leaving mangled leftover text (e.g. "read-write</code>} id=\"authorize-scope\">") in place of clean prose. Broader than it first looked: ALL `defaultValue` usages on `<Parameter>` were silently losing that attribute's content entirely (whether wrapped in a nested `<code>` tag or a plain string) — same root cause as DocsLink/McpConfigBlock (prop-carried content, not children, vanishes with the tag). Scope: 33 occurrences across exactly 3 files, connect/api/{events,oauth,webhooks}.md (docs/angular/datepicker/return-value.md's one `defaultValue` hit was `<Tabs defaultValue="ts">`, unrelated, already covered by the accepted Tab-loss tradeoff). Fixed with the established marker technique: {/* llms:param;default=VALUE */} / {/* /llms:param */} around each <Parameter>...</Parameter> block (value extracted from either the {<code>...</code>} or plain-string form at marker-insertion time), strip-jsx.js reconstructs the description body (filtering out the mangled-leftover line by shape, `^[^<]*</code>\}[^<]*>$`, if present) followed by a clean "**Default:** `value`" line. One edge case caught on first build attempt: a Parameter with a trailing `isObject` attribute after `id="..."` didn't match the initial mangled-leftover filter regex (which assumed `id="...">` was the immediate line end) — widened to `[^<]*>$` to tolerate any trailing attributes. Verified: all 33 Default lines present, zero mangled leftovers or stray markers across every generated llms-*-full.txt
learnings: this component-attribute-loss failure shape (prop content silently dropped when a wrapping tag is stripped) has now recurred three times (DocsLink et al., then this) with three different concrete symptoms (missing link, missing JSON, mangled leftover text) — worth remembering as the single most common failure mode from 0.5.1's blanket tag-stripping, so future audits should specifically grep for `\w+=\{?<` (any prop whose value starts with a JSX tag) rather than only self-closing components
---

---
timestamp: 2026-08-18T14:00:00Z
action: corrected the Parameter fix — the user caught that only `defaultValue` had been addressed, not the `name`/`type`/`required` header that every `<Parameter>` also loses
context: user manually compared the live rendered page (screenshot: "client_id string REQUIRED" header + description) against the llms-connect-full.txt output (screenshot: description text with no name/type/required header at all) and pointed out the mismatch
outcome: re-scoped from the 33 defaultValue-only occurrences to ALL <Parameter> usage — 231 occurrences across 11 files (connect/api/{calendars,events,oauth,webhooks}.md + connect/integration/{dotnet,go,java,node,php,python,ruby}-sdk.md). Reverted the narrower 33-occurrence fix from the prior entry and replaced it with a comprehensive one: marker now carries name/type/required/default ({/* llms:param;name=X;type=Y;required=1;default=Z */}), and strip-jsx.js rebuilds a full header ("**name** (`type`, required)") before the description, plus the "**Default:**" line after, same as before
outcome (nesting): Parameter blocks nest up to 3 deep (an object-typed parameter listing its own sub-fields, e.g. conferenceData → provider) — rewrote the flat single-frame reconstruction as a stack: each closing marker pops its frame, rebuilds it, and appends the result into the *parent* frame's body (or straight to `out` if top-level), so nested reconstructions land in the right place
outcome (ordering fix): found during testing that 4 Parameter descriptions contain their own fenced code blocks (```js title="Example"``` samples). The existing fenced-code-block tracking ran *before* the param-marker logic in the line loop, so those fence lines would have been pushed straight to `out`, bypassing the open frame entirely and landing out of order. Moved the whole llms:param marker check (open/close/redirect-line-to-current-frame) to the very top of the loop, ahead of fenced-block tracking, so fenced content inside a Parameter's description is correctly captured into that frame's body instead
outcome (verification): full rebuild — 231 headers and 33 Default lines present in llms-connect-full.txt, all 7 integration SDK pages show their expected header counts (dotnet/go/java/php/python: 6, node: 7, ruby: 8), zero stray {/* llms:param */} markers or mangled </code>} leftovers anywhere across all 6 generated llms-*-full.txt files, nested conferenceData→provider and the fenced-code-inside-Parameter case both verified rendering correctly in isolation before the full build
learnings: don't declare a fix complete from a partial repro — I saw the mangled-leftover symptom (defaultValue), fixed exactly that, and didn't re-derive from the component's actual render output (name + type + required + description + defaultValue, all five) what the *complete* set of lost information was. The user's side-by-side screenshot caught what a text-only diff of the malformed line had let me miss. When fixing a "component prop gets dropped" bug, enumerate every prop the component actually renders, not just the one whose absence produced visible mangled text
---

---
timestamp: 2026-08-18T15:00:00Z
action: explained why `import moment from 'moment-timezone';` (one of the accepted-loss import lines from an earlier entry) actually survives in llms-angular-full.txt
context: user asked how that one line escaped the resolvePartialImports strip described as unfixable earlier
outcome: traced it to an accident of authoring: that specific line is the *first* line inside a <CodeBlock>{`...`}</CodeBlock> template literal, so its actual source text starts with a backtick, not the literal word "import" — the destructive regex `/^\s*import\s+.*$/gm` requires "import" as the very first token and never matches. The *next* import lines in the same block have no such shield and were still being deleted. Confirmed the deciding factor precisely: `/^\s*import\s+.*$/.test("\`import moment...")` → false (backtick blocks it), same regex against a real fenced-block import line → true (deleted)
learnings: this is a coincidence of formatting, not a working fix — moment/luxon/dayjs_install.mdx remain part of the accepted import-line-loss scope from the earlier entry
---

---
timestamp: 2026-08-20T12:00:00Z
action: mirrored the marker fixes into versioned_docs/version-5.35.0, standardized excludeImports across all three plugin configs, and found + fixed a live CRLF bug affecting the entire versioned_docs tree
context: user asked to sync docs/ with versioned docs; investigation found version-6.1.0 was already 100% mirrored (verified byte-for-byte, 42/42 files match — no work needed there, and no llms-plugin instance even reads that folder), but version-5.35.0 had zero markers despite llms-v5-plugin.config.js actively building real 5.35.0/llms-*.txt output from it every build. Also investigated a user-reported print-module import discrepancy between production v5 and v6 pages — traced to the *deployed* 0.3.0-patched plugin (irrelevant to this 0.5.1 branch), then separately confirmed via direct testing that `excludeImports` (inconsistent across configs: docs=false, connect=true, v5=true) has zero relationship to any content-loss bug — it only strips plumbing import lines outside masked code segments, verified with a real test script against the plugin's actual cleanMarkdownContent
outcome (v5.35.0 markers): applied the same three marker categories used for docs/ — `<!-- llms-fence -->` around the 12 _shared/{getting-started,guides,theming,timezones}/*.mdx CodeBlock partials (19 fences), `{/* llms:blockremove */}` around all 150 ImgComparisonSlider usages across the 25 eventcalendar pages (5 frameworks × agenda/calendar/scheduler/templating/timeline), and `{/* llms:docsurl|docslink|mcpconfig|mcpcli|fileblock */}` around all 35×5=175 AI-integration components across the 5 frameworks' ai-integration.md. Verified marker counts match docs/ exactly for every category before rebuilding
outcome (v5-only pages audit): found and audited 10 pages that exist in version-5.35.0 but were dropped from docs/version-6.1.0 (`{framework}/guides/{upgrade-guide-v5,usingv4-with-v5}.md` × 5). `usingv4-with-v5.md` wraps a two-line prose-only partial (no risk); `upgrade-guide-v5.md` uses only Tabs/TabItem (already-accepted loss) and self-closing `<Datepicker controls={...} />` demo widgets with no textual payload — no fixes needed for either
outcome (excludeImports): standardized to `true` across all three plugin configs (was `false` in llms-plugin.config.js, already `true` in connect and v5) — confirmed via build diff that this only removes plumbing import lines (e.g. `import Tabs from '@theme/Tabs'`) outside code samples, zero impact on any code-sample content
outcome (CRLF bug — found and fixed, NOT part of original plan): full rebuild after the marker pass showed a real `<CodeBlock>` tag leaking raw into `5.35.0/llms-{angular,javascript,jquery,react,vue}-full.txt`'s "Timezones(Datepicker)" section, while the equivalent "Timezones" (eventcalendar) section right next to it was clean. Traced via isolated resolvePartialImports+cleanMarkdownContent testing to `versioned_docs/version-5.35.0/react/datepicker/timezones.md` having CRLF line endings throughout (89 CR bytes) — the exact original 0.5.1 CRLF bug from the very start of this branch's work (maskCodeSegments' closing-fence lookahead `(?=\n|$)` fails on `\r\n`, causing the fence-masking regex to non-greedily over-extend past the failed closing fence until it finds a later, properly-LF-terminated one, swallowing our llms-fence markers and the CodeBlock tag into one opaque masked token that gets restored verbatim, unstripped). Root cause: the earlier `.gitattributes` LF-forcing fix only covered `docs/**` and `connect/**`, never `versioned_docs/**` — a scan found 9,661 of 9,671 tracked versioned_docs md/mdx files had CRLF on disk (a systemic, repo-wide local-checkout artifact, not confined to this one file). Flagged to user as a scope question (fix just the one file vs. the whole tree) — user chose the full fix
outcome (CRLF fix applied): broadened `.gitattributes` to add `versioned_docs/**/*.md` and `versioned_docs/**/*.mdx` (both `text eol=lf`), ran `git add --renormalize versioned_docs/` (scoped, reviewed — only the 84 already-modified files showed additional diff, confirming everything else was already LF at the blob level despite local CRLF checkout), then normalized the actual working-tree files (git renormalize only touches the index, not disk) via a direct read/write script stripping `\r\n` → `\n` across all 9,661 affected files. Verified zero CR bytes remain, all marker content intact, full rebuild clean — zero raw tag leaks, zero stray markers, across every generated file in build/
incident: mid-fix, a chained shell command (`git checkout-index -f -a -- versioned_docs/ || git stash -u -- versioned_docs/`) failed on the first half and the `||` fallback executed, stashing all 84 uncommitted versioned_docs changes. Caught immediately via `git stash list`, popped back with `git stash pop`, verified full restoration (same 84 files, same content) before continuing — no work lost, but flagged transparently to the user in the moment per the git-safety protocol
learnings: (1) a "verified" migration should test the actual affected page structure, not just the shared partial in isolation — the eventcalendar/timezones.md vs datepicker/timezones.md divergence with byte-identical spliced partial content was the tell that something environmental (not content-level) was responsible; (2) `git add --renormalize` only updates the index — it does NOT rewrite working-tree files, so a real fix still requires touching disk directly (or a checkout), which is a strictly more dangerous operation than a plain read/write script when other uncommitted changes are in play; (3) never chain a destructive fallback (`|| git stash ...`) after a command whose failure mode isn't fully known — a `||` fallback runs on ANY non-zero exit, not just the specific error anticipated
---

---
timestamp: 2026-08-20T14:00:00Z
action: fixed v5.35.0's individual per-page .md twins being silently unreachable (collided with and were overwritten by the main docs plugin's own output at the same build paths); found and deferred a deeper, three-part link-correctness problem this fix exposed
context: user asked how to reach v5 llms files, prompting a check of build/5.35.0/ — found only the aggregate llms-*.txt files existed there; per-page .md twins from llms-v5-plugin.config.js's generateMarkdownFiles were landing at unprefixed build/{framework}/... paths (same as the main docs plugin's own output), silently overwritten since both plugins target the same outDir and only one write survives. User asked to fix it
outcome (fix applied): converted llms-v5-plugin.config.js from a flat `docsDir` + manually "5.35.0/"-prefixed customLLMFiles filenames to the plugin's native `versions` option (`versions: [{name: '5.35.0', docsDir: 'versioned_docs/version-5.35.0', path: '5.35.0', customLLMFiles: [...unprefixed filenames...]}]`), which auto-prefixes both aggregate and per-page output via an internal `outputSubdir`. Verified: build/5.35.0/react/eventcalendar/timezones.md now exists as its own file with correct v5 content, main docs plugin's build/react/eventcalendar/timezones.md is unaffected, no raw tag/marker leaks
outcome (new problem found, NOT fixed — deferred with user's agreement): rebuilding surfaced that links *inside* generated v5 content are largely unversioned (e.g. a v5 page links to its own v6/unversioned URL instead of the 5.35.0 one). Root-caused to three stacked issues: (1) the plugin's `versions`/`routePrefix` scoping assumes routesPaths exclude the site's baseUrl, but this site's baseUrl is '/docs' so real routes include it (`/docs/5.35.0/react/...`) — the scoping filter never matches, silently falls back to unscoped suffix-matching across the whole site, which always prefers the shortest (unversioned) route when a page exists in both versions; (2) the known per-plugin workaround for this (`pathTransformation.addPaths: ['5.35.0']`) does fix the links, but since `generateIndividualMarkdownFiles` derives each file's write path from that same resolved URL, combining it with `versions`' own `outputSubdir` double-prefixes the path (`build/5.35.0/5.35.0/...`) — confirmed by testing both together; (3) `scripts/strip-jsx.js`'s own marker reconstruction (llms:docsurl/mcpconfig/fileblock/docslink) uses one hardcoded `DOCS_BASE_URL` constant regardless of which version's build subtree a file lives in, so anything WE reconstruct is unversioned too. Also spotted, not investigated: a `docs/docs/` doubled-path artifact in a handful of v5 links (likely unrelated to the above three)
outcome (decision): presented the tradeoff to the user (ship the reachability fix alone vs. attempt a fast but risky post-build regex patch). User chose to stop at the reachability fix and requested a copy-pasteable task writeup for the link-correctness work to resume later — provided in-conversation, not persisted to a separate file (repo convention avoids planning-doc files; this log entry is the durable record)
learnings: fixing a "files aren't reachable" symptom can uncover that the content underneath was already broken in a way nobody could previously observe (nothing was reachable to check) — a reachability fix and a content-correctness fix are separable, and shipping the former without silently attempting the latter under time pressure was the right call given the latter's real complexity (three independent root causes, one of them in code shared with every other build)
---
