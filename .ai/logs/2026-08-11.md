# Agent Log — 2026-08-11 — main — investigate large versioned_docs/version-6.1.0 diff after re-running docs:version

---
timestamp: 2026-08-11T00:00:00Z
action: investigated why `npm run docusaurus docs:version 6.1.0` produced ~886 modified files under versioned_docs/version-6.1.0
context: user request to explain the diff and trace it to the Docusaurus v3 migration
outcome: |
  Classified the 886 changed files: 594 are byte-identical to HEAD (git status false positive,
  stat-cache/CRLF noise — core.autocrlf=true with no eol=lf override for versioned_docs paths in
  .gitattributes, unlike docs/**/_auto-generated which has one). 141 are trailing-newline-only
  additions. 151 carry real content changes: HTML-entity unescaping (&#123;/&#125; -> {}), unescaped
  underscores in import paths, and an "[MbscLocale](#type-MbscLocale)" link reverting to plain text.
  Root cause: commit 07dde5d7 ("fix(llms): fix strip-jsx post-build script for Docusaurus v3 MDX
  compatibility") touched both docs/ and versioned_docs/ but applied opposite entity-escaping
  directions to the two trees, leaving docs/ (unescaped) and versioned_docs/version-6.1.0 (escaped)
  out of sync ever since. Re-running docs:version simply re-syncs the snapshot from current docs/,
  surfacing that drift as a large diff. Separately found unrelated, pre-existing dirty changes in
  docusaurus.config.js, version-map.json, and package-lock.json that blindly replace the literal
  string "6.1.0" with "5.35.0" (lastVersion, versions map, onlyIncludeVersions, and even an unrelated
  npm package's version field) — not caused by docs:version (native docs:version does not touch these
  files; versions.json itself has no diff). Flagged to user as a likely leftover from a manual/half-done
  version rollback, unrelated to today's docs:version run.
learnings: |
  .gitattributes only pins `docs/*/_auto-generated/**/*.md` to `eol=lf`; versioned_docs copies of the
  same generated files have no such rule, so on Windows with core.autocrlf=true git status can flag
  large swaths of versioned_docs as modified with zero real content difference (verify via
  `git hash-object` vs `git rev-parse HEAD:<path>` before trusting `git status` on this tree).

---
timestamp: 2026-08-11T00:15:00Z
action: mapped the 151 real-change files to public /docs/ URLs and categorized by fix type
context: user requested relative doc URLs for the pages affected by the toc/heading, {count}-entity, and
  "any suspicious thing" changes found in the version-6.1.0 diff
outcome: |
  Resolved 130 unique affected pages (auto-generated partials mapped to their importing api.md/page via
  grep of import paths). Categories: heading-html-to-md (raw <h2/h3/h4 id="..."> -> markdown headings,
  25 pages, affects the auto-generated right-sidebar ToC), entity-curly-count (&#123;count&#125; -> {count}
  inside code, 25 pages), backslash-underscore (cosmetic, invisible MDX import escaping, 95 pages), and one
  flagged-as-suspicious mojibake/BOM fix isolated to react/datepicker/controls.md (stray UTF-8 BOM at file
  start + a double-encoded em-dash "â€"" in the frontmatter description, from real corruption, not migration
  noise). Verified compatibility empirically: ran `npm run build-localdev` — build succeeded (exit 0) for
  both version-6.1.0 and version-5.35.0; only warning was a pre-existing, unrelated set of broken anchor
  links from current docs pages into version-5.35.0's select/api (predates this session's changes).
learnings: |
  llms-plugin.config.js sources only from `docs/` (main plugin) and `versioned_docs/version-5.35.0`
  (dedicated llms-v5-plugin.config.js) — version-6.1.0 is not a source for any llms plugin, so content
  drift inside versioned_docs/version-6.1.0 cannot affect generated llms*.txt files. scripts/strip-jsx.js
  additionally normalizes &#123;/&#125; entities and strips `export const toc` lines in build/ output
  regardless of source state, as a second independent safety net for the LLM surface specifically.

---
timestamp: 2026-08-11T00:30:00Z
action: planned (not yet implemented) how to apply the same fix categories to versioned_docs/version-5.35.0
context: user asked for a plan to replicate the 6.1.0 content fixes on version-5.35.0, which cannot be
  re-synced from docs/ (docs/ now represents the newer 6.1.0+ API surface, structurally different from 5.35.0)
outcome: |
  Investigated version-5.35.0 directly (via Explore + Plan subagents) rather than assuming parity with
  6.1.0. Confirmed only 2 of the 6 issue categories actually apply: (1) &#123;/&#125; over-escaping, but
  only ~40 of 73 files where the escaped braces sit inside a fenced code block or inline backtick span —
  the other 33 files' braces are in prose/italic type-signature text and must stay escaped or MDX3's acorn
  parser breaks the build (existing scripts/fix-mdx-issues.py already guards this direction, escape-only,
  never unescape); (2) raw <h2/h3/h4 id="..."> tags, ~25 files, explicitly excluding
  eventcalendar/calendar-integrations/calendar-integrations.md's <h3 id="..." className="api-heading">
  variant, which is intentional (confirmed current docs/ still uses that exact form for font-size styling
  markdown headings can't express). Backslash-underscore imports, MbscLocale link bug, BOM/mojibake, and
  _shared toc-suppression categories: zero matches in version-5.35.0, not applicable. Plan: extend
  scripts/fix-mdx-issues.py (reuse its --dry-run/--path CLI) with two new context-aware transforms rather
  than write a new script or blanket find-replace. Plan written to
  C:\Users\IstvanCsiszer\.claude\plans\show-me-a-plan-shimmering-prism.md, not yet executed per user
  instruction (Phase 1 deferred).
learnings: |
  Never assume an issue category found in one versioned snapshot applies identically to another — the
  same-looking &#123;...&#125; escaping serves opposite purposes depending on version/context (over-escaping
  bug in 6.1.0's inline-code occurrences vs. required MDX3 safety escaping in 5.35.0's prose/type-signature
  occurrences). Always verify per-version via direct file inspection before generalizing a fix.

---
timestamp: 2026-08-11T00:45:00Z
action: staged (not committed) the 292 real-diff files under versioned_docs/version-6.1.0
context: user wants to commit only the real content changes now, manually, and asked to stage real-changed
  files (content changes + trailing-newline additions) while leaving the 594 line-ending-only false
  positives unstaged for a separate future fix
outcome: |
  Computed the file set by comparing `git hash-object <path>` against `git rev-parse HEAD:<path>` for every
  file git status flagged under versioned_docs/version-6.1.0/ — 292 files differ (151 real content + 141
  trailing-newline-only), 594 are byte-identical (left unstaged). Ran `git add` on exactly those 292 files.
  Verified: `git diff --cached --name-only -- versioned_docs/version-6.1.0/` = 292 files; no unrelated files
  (docusaurus.config.js, version-map.json, package-lock.json) were staged. Did not commit — user will commit
  manually. Proposed commit message (see below) per SYSTEM.md § 5 format.
learnings: |
  When "real change" needs a precise, reproducible definition (not just "git status says modified"), hash
  comparison against HEAD is the reliable filter — apply it before any git add on a tree with CRLF/eol
  ambiguity, rather than trusting git status output directly.

---
timestamp: 2026-08-11T01:15:00Z
action: implemented Phase 1 of the plan — extended scripts/fix-mdx-issues.py, applied to versioned_docs/version-5.35.0
context: user asked to implement the previously-drafted plan (not commit it) — apply the same content-fix
  categories to version-5.35.0 that were already applied to version-6.1.0
outcome: |
  Added two new functions to scripts/fix-mdx-issues.py, wired into run_fixes(): fix_code_context_entities()
  (unescapes &#123;/&#125; only inside a fenced code block or a same-line single-backtick span, tracked
  line-by-line like scripts/strip-jsx.js does for fences) and fix_html_headings() (converts bare
  <h2/h3/h4 id="..."> tags with no other attributes to markdown ATX headings with anchor syntax; the
  anchored regex naturally excludes the className="api-heading" variant in calendar-integrations.md
  without needing a special-case exclusion). Both preserve each file's original CRLF/LF line endings via
  `newline=""` read/write + `splitlines(keepends=True)`.
  Smoke-tested against the two known contrasting examples (localization.md unescapes, types.md correctly
  left untouched), then dry-ran against the full version-5.35.0 tree: 65 files matched (40 code-entity,
  25 heading-conversion), zero touching calendar-integrations.md — exactly as predicted. Applied for real.
  git diff confirms 65 files changed, 217 insertions / 217 deletions (symmetric — pure line replacements,
  no line-ending regressions). Ran `npm run build-localdev`: [SUCCESS], zero warnings at all this time
  (even the previously-seen pre-existing v5 select/api broken-anchor warnings from the 6.1.0 session are
  gone from this build's output). Not committed, per user instruction — left in the working tree.
  Also mapped the 65 changed files to their public /docs/5.35.0/... URLs (partials resolved to importing
  pages the same way as the 6.1.0 mapping) so the user can manually verify via local build + serve.
learnings: |
  version-5.35.0 is not the Docusaurus "lastVersion" (that's 6.1.0, served at site root per
  docusaurus.config.js's `versions: { "6.1.0": { badge: false } }` / `lastVersion: '6.1.0'`), so all its
  routes carry an explicit /5.35.0/ path segment — unlike 6.1.0's URLs, which have no version prefix.
  Always check `lastVersion`/`versions` config before assuming a versioned doc's URL shape.
---
