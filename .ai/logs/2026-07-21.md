# Agent Log — 2026-07-21 — main — backport "From" version badge into versioned_docs/version-6.1.0

---
timestamp: 2026-07-21T00:00:00Z
action: repeated git worktree cleanup — discarded CRLF-only noise and untracked spark-sync artifacts
context: spark project's external sync script re-ran the docs generator multiple times during the session,
  each time re-touching line endings across all ~450 docs/**/_auto-generated/*.md files (no real content
  change) and recreating three new untracked component folders (draguladraggable/, mbscexternaldropevent/,
  sortablejsdraggable/) under docs/{fw}/_auto-generated/ for all 5 frameworks
outcome: |
  Repeated cleanup cycle (~6 times over the session, since the sync script kept re-firing between checks):
  git checkout -- on files where `git diff --name-only` showed no real content diff (pure CRLF noise vs HEAD),
  and `git clean -fd` on the untracked new-component directories. Left only files with genuine content diffs
  as `M` in git status each time. Per CLAUDE.md workflow table, docs/**/_auto-generated/** changes are
  "detection only" (spark-owned) — no content was authored in docs/ itself during these cleanups.
learnings: the reusable one-liner for this cleanup:
  git checkout -- $(comm -23 <(git status --porcelain | grep '^ M' | sed 's/^ M //' | sort) <(git diff --name-only | sort) | tr '\n' ' ')
  — subtracts files with a real `git diff` from all modified-in-worktree files, leaving pure line-ending noise
  to restore. Untracked dirs need a separate `git clean -fd <paths>` pass; they aren't touched by checkout.
---

---
timestamp: 2026-07-21T00:15:00Z
action: backported "From: X.Y.Z" version badge and doc-wording fixes from docs/ into versioned_docs/version-6.1.0
context: spark's generator started emitting `<span className="badge badge--secondary margin-bottom--sm">From: 6.1.0</span>`
  markers (matching the Docusaurus DocVersionBadge style, `badge badge--secondary`, discussed earlier in-session)
  into several current-docs/ _auto-generated files. Since docusaurus.config.js pins lastVersion to "6.1.0" and
  the live/served snapshot for that version lives in versioned_docs/version-6.1.0 (not docs/, which is the
  working copy for the next cut), the new badges and wording fixes were missing from the version actually
  served at that version. User asked to mirror the same real changes into versioned_docs/version-6.1.0.
outcome: |
  Diffed each changed docs/**/_auto-generated file against its versioned_docs/version-6.1.0 counterpart with
  line endings normalized (`diff <(tr -d '\r' < a) <(tr -d '\r' < b)`) to isolate genuine content differences
  from CRLF/LF noise, then applied only the real delta via targeted Edit calls (never a raw patch/git-apply,
  since versioned snapshots have diverged enough from docs/ that whole-file patches didn't apply cleanly).
  Edited 15 files across angular/javascript/jquery/react/vue in versioned_docs/version-6.1.0:
  - Inserted the "From: 6.1.0" badge span into datepicker/popup/select renderers.md (all 5 frameworks)
  - Updated angular eventcalendar/renderers_agenda.md's agenda template example to match docs/ (see below)
  Confirmed 4 angular eventcalendar/events*.md wording tweaks needed no versioned-docs change — that wording
  was already present in the version-6.1.0 snapshot before this session started.
  Re-ran the same comparison a second time after the spark script produced further updates: found one file
  (angular/_auto-generated/datepicker/renderers.md) missed in the first pass (its own separate "headerTemplate"
  badge), added it; and found renderers_agenda.md needed re-syncing again because the spark script's own
  output flipped from HTML-entity-escaped markup (`&lt;li&gt;...&lt;/li&gt;`) with a broken closing-tag
  regression (`</li&gt;` instead of `&lt;/li&gt;`) back to clean raw tags (`<li>...</li>`) — updated the
  versioned copy to match the now-corrected docs/ content.
learnings: |
  - versioned_docs/version-6.1.0 is the actually-served snapshot for the pinned lastVersion in
    docusaurus.config.js, while docs/ is a live working copy receiving continuous spark syncs — the two can
    drift, and "docs changed" does not imply "the served version already has it." Any spark-sync content
    affecting the current release needs an explicit backport pass into versioned_docs/version-6.1.0, since
    Workflow B (auto-generated file update) only covers docs/ and is detection-only by design.
  - The spark sync script is not idempotent within a session — it re-ran several times, sometimes changing
    its own prior output (the renderers_agenda.md escaping flip-flop). Backport comparisons should be redone
    after every spark re-run rather than assumed stable from an earlier diff.
---
