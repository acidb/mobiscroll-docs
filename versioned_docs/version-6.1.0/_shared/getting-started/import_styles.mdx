<span>For the components to work, you will also need to import the css file of the installed library
 under the <code>node_modules</code> folder
 (at <code>@mobiscroll/{props.framework}/dist/css/mobiscroll.min.css</code>).
</span>

If you are [using SCSS](https://sass-lang.com/), you will also find a full and a modular `.scss` file in the same directory.
