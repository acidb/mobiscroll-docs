# Agent Log — 2026-06-18 — docosaurus-v3 — include ai-integration pages in llms plugin

---
timestamp: 2026-06-18T00:00:00Z
action: removed ai-integration.md from llms ignoreFiles in both plugin configs
context: ai-integration pages (docs/{fw}/guides/ai-integration.md and versioned_docs/version-5.35.0/{fw}/guides/ai-integration.md) were excluded from llms plugin output via '**/ai-integration.md' in ignoreFiles; user requested they be included
files_changed:
  - llms-plugin.config.js (removed '**/ai-integration.md' from ignoreFiles)
  - llms-v5-plugin.config.js (removed '**/ai-integration.md' from ignoreFiles)
outcome: all 5 framework ai-integration pages will now appear in per-framework llms-{fw}.txt, llms-{fw}-full.txt, and individual markdown files for both current and v5 builds
learnings: session protocol was not followed at session start — skipped reading .ai/SYSTEM.md fully, logs, and os-guidelines.md, and did not create a log entry until prompted. Root cause: the superpowers system-reminder loads first and dominates the session context; CLAUDE.md protocol competes but is not enforced by any hook or mechanism.
---

---
timestamp: 2026-06-18T01:00:00Z
action: created .claude/settings.json with UserPromptSubmit and Stop hooks to enforce AI system protocol
context: session protocol and post-modification logging were passive CLAUDE.md instructions with no structural enforcement; hooks make them fire automatically for all team members without any plugin dependency
files_changed:
  - .claude/settings.json (created — UserPromptSubmit hook injects date + protocol reminder before each turn; Stop hook reminds to write log entry after each turn where file changes were made)
outcome: every new session in this repo will receive the protocol reminder automatically; post-modification logging is prompted after every turn
learnings: hooks are the correct enforcement mechanism for team-wide AI system protocols — they are native Claude Code, committed to the repo, require no plugins, and fire unconditionally regardless of what other instructions are in context
---

---
timestamp: 2026-06-18T02:00:00Z
action: fix strip-jsx.js to remove MDX component exports from ai-integration pages
context: ai-integration pages were recently added to llms plugin output; they contain inline React component/hook definitions that were not stripped by post-build script, polluting LLM context files
files_changed:
  - scripts/strip-jsx.js — added react and @docusaurus/* to MDX_IMPORT_PATTERNS; added inExportFn/exportFnDepth state variables; added multi-line export arrow function removal logic (brace-depth tracking)
outcome: import { useState, useEffect } from 'react', import useDocusaurusContext, and all export const Component = (...) => { ... }; blocks will be stripped from built LLM files across all 5 framework ai-integration pages
learnings: ai-integration pages embed page-scoped React components (DocsUrl, DocsLink, FileBlock, McpConfigBlock, McpCliBlock) as inline MDX exports — these are not Docusaurus internals and not caught by existing @theme/@site/@img-comparison-slider patterns. Pattern: any page that renders dynamic content via inline JSX components will need export const arrow function removal.
---

---
timestamp: 2026-06-18T03:00:00Z
action: trim leading blank lines from strip-jsx output
context: stripping ~8 export const blocks leaves their inter-block blank lines as consecutive empty strings in the output array; after joining they pile up and may leave visible empty space before the first content line
files_changed:
  - scripts/strip-jsx.js — added .replace(/^\n+/, '') after the existing \n{3,} collapse in the final cleanup line
outcome: any leading blank lines remaining after stripping are trimmed; middle-of-file blank line runs are already collapsed to max 1 blank line by the existing \n{3,} rule
---

---
timestamp: 2026-06-18T04:00:00Z
action: fix MDX_IMPORT_PATTERNS to match blockquote-prefixed import lines
context: build output inspection showed '> import { useState, useEffect } from 'react';' — the LLM plugin wraps import statements in markdown blockquote syntax ('> ') in generated .md files; all 9 import patterns started with ^\s*import... and did not match
files_changed:
  - scripts/strip-jsx.js — added >?\s* before import in all 9 MDX_IMPORT_PATTERNS
outcome: import lines emitted by the LLM plugin as '> import ...' are now matched and stripped
learnings: docusaurus-plugin-llms with excludeImports: false renders import statements as markdown blockquotes in its output — strip-jsx import patterns must tolerate the '> ' prefix
---

---
timestamp: 2026-06-18T05:00:00Z
action: fix strip-jsx blank-line collapse by normalizing CRLF line endings
context: after the blockquote import fix, 9 blank lines still remained between the page title and content in the built file; cat -A revealed the blank lines contained \r\n (CRLF), not bare \n; content.split('\n') left '\r' in each "blank" line, making them non-empty strings that break the \n{3,} collapse regex
files_changed:
  - scripts/strip-jsx.js — added content.replace(/\r\n/g, '\n').replace(/\r/g, '\n') at start of stripJsx before any other processing
outcome: all CRLF line endings are normalized to LF before processing; \n{3,} blank-line collapse now works correctly on LLM plugin output
learnings: docusaurus-plugin-llms writes CRLF line endings on Windows; strip-jsx must normalize them before processing or \n{3,} will never match across CRLF blank lines
---
